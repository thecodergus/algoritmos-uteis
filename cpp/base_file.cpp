#include <iostream>

#define F(x) for(int i = 0; i < x; i++)
#define pb push_back
#define Fe(x) for(auto i : x)

using namespace std;

int main(){
    ios::sync_with_stdio(0);
    std::cin.tie(0);


    return 0;
}